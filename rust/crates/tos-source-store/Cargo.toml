[package]
name = "tos-source-store"
version = "0.1.0"
edition = "2024"
publish = false

[features]
default = []

[dependencies]
tos-foundation = { path = "../tos-foundation" }
rustix = { version = "=1.1.5", features = ["fs"] }
