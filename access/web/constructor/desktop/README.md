# Древо Софии — отдельное локальное приложение

Этот запуск открывает философский макет в отдельном окне Chromium. Он использует
собственный профиль и сохранённое рабочее пространство. Обычные окна и профили
браузера, старый стенд на порту 44338 и сохранённый `snapshot-v1` остаются отдельными.
Это локальная упаковка демо, без публикации и без официальной подписи приложения.

## Подготовка

Нужны Linux, пользовательский systemd, Python 3.11+, Chromium и установленный
владелец ресурсов `abyss-machine`. Для установки требуется `desktop-file-validate`.
При ошибке запуска показывается диалог Zenity, если он установлен; иначе причина
остаётся в журнале и stderr. Зависимости установщик не устанавливает.

Сначала соберите и проверьте **неизменяемый** релиз в
`/srv/abyss-machine/storage/artifacts/tos-sophia-demo/releases/meaning-v2`.
В нём нужны `constructor.html`, `library.json` и каталог `assets/`.
Установщик запоминает точный путь и SHA-256 всех файлов, доступных по HTTP.
Символические ссылки для переключения релиза не используются.

```sh
python3 access/web/constructor/desktop/install.py
# Проверив показанные пути:
python3 access/web/constructor/desktop/install.py --install
```

Установка добавляет запись «Древо Софии — демо» в меню приложений, компактную
команду `~/.local/bin/tos-sophia-demo`, конфигурацию и службу пользователя.
Она не запускает сервер или браузер, не включает автозапуск и не создаёт профиль
или дисковый кэш. Перед первым запуском владелец хоста должен допустить и
зарезервировать их запись: профиль напрямую в
`/srv/abyss-machine/runtimes/tos-sophia-demo/profile`, кэш отдельно в
`/srv/abyss-machine/cache/tos-sophia-demo`. Лимит дискового кэша Chromium — 32 MiB;
размер профиля зависит от Chromium и сохранённых пользовательских данных.

## Запуск и проверка

Откройте «Древо Софии — демо» из меню приложений. Для проверки без открытия окна:

```sh
~/.local/bin/tos-sophia-demo --dry-run
~/.local/bin/tos-sophia-demo --ensure-server
~/.local/bin/tos-sophia-demo --status
```

`--ensure-server` возвращается после проверки готовности, обычно за несколько
секунд, с предельным ожиданием 45 секунд. Сервер остаётся в службе пользователя
`tos-sophia-demo.service`. Он слушает только `127.0.0.1:44339` и отдаёт только
файлы релиза. Проверка сверяет идентичность приложения, точный релиз, его digest,
PID и команду внутренней службы. Посторонний процесс на этом порту не принимается
за демо и не останавливается.

Служба держит штатный `abyss-machine resource launch`; его дочерний HTTP-процесс
пишет stdout/stderr напрямую в `server.log`, stdin направлен в `/dev/null`.
Закрытие команды проверки не обрывает HTTP. Запуск нового окна Chromium также
проходит через владельца ресурсов, с отдельной оценкой памяти. Отказ допуска
не обходится параметрами `--force` или отключением sandbox.

Проверить журнал и явно остановить только сервер этого приложения:

```sh
journalctl --user -u tos-sophia-demo.service -n 30
systemctl --user stop tos-sophia-demo.service
```

Журналы `server.log`, `browser.log` и `launcher.log` расположены под
`/srv/abyss-machine/runtimes/tos-sophia-demo`. Остановка сервера не закрывает
браузер; вкладка сможет продолжить работу после следующего запуска сервера.
Служба не включена на вход в систему и завершается вместе с сеансом пользователя.

## Другой релиз и перенос

Для обновления сначала сохраните прежний релиз и пользовательское рабочее
пространство, затем явно остановите эту службу. Укажите новый проверенный каталог:

```sh
python3 access/web/constructor/desktop/install.py --release /absolute/path/to/release
python3 access/web/constructor/desktop/install.py --release /absolute/path/to/release --install --replace
```

Изменение файлов уже запущенного релиза не является обновлением: launcher
обнаружит несовпадение digest. Установщик откажется менять активную установку.
Профиль и кэш при обновлении не удаляются. Для другого Linux-хоста доступны
параметры путей установщика; маршрут resource admission должен существовать и
при переносе. `server.py` использует только стандартную библиотеку Python.

## Проверка исходников

`test_desktop.py` проверяет реальный HTTP allowlist и идентичность, отказ при
изменившемся релизе, непринятие чужой службы, отсутствие запуска при dry-run,
разделение профиля/кэша и синтаксис сгенерированной desktop-записи.
Эти проверки не открывают браузер и не устанавливают службы.
