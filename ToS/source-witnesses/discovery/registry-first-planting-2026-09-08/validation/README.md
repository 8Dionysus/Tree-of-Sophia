# Local validation evidence

The preparation generated-parity sequence and source-foundation check passed
before acquisition. The first canary log records the actual failed companion
checks; its successor records the successful repaired state. The original
payload bytes did not change. The batch source-foundation check includes all
308 newly present source files; older ignored payloads absent from this isolated
checkout are not claimed as locally verified.

The normalization result is reproduced from all retained originals. Focused
regressions and the final generated-parity/currentness review are recorded at
closeout against the local commit. These are local mechanical checks, not CI,
merge, deployment, textual/semantic acceptance or publication permission.
