# Archaic Vessels and Garments Composite

This directory identifies the modern scholarly composite known as DCCLT/CDLI
`Q000023`, also represented by CDLI composite record `P471693`.

The tracked packet is metadata-only. It records one exact member observation
for the already planted physical witness `P000015`, current provider
fingerprints, representation coverage differences, and the DCCLT license. It
does not copy the composite text, member transliterations, or images into Git.

CDLI's full composite API currently exposes 98 witnesses and contains
`P000015`. The current DCCLT Q-page exposes 36 source links and does not show
`P000015` in that page list. Those are preserved as different observed
representations; the shorter page list is not silently treated as a complete
membership denial.
