# Bounded source record revision and exact retained packages

Scope: source/Growth implementation, reviewed by `model:codex` on 2026-09-07.
This records engineering inspection, not historical assessment or permission
to publish. Runtime session: `01a06cc7-0452-77f2-b89a-fb77fb86c3bf`.

## Changed owner surfaces

The source command contract now separately delegates historical-record field
correction. `source_revisions.py` publishes the record, explicit form successors
and retained request/receipt in one directory exchange. Original package bytes
are independently checked in a source-owned blob archive before replacement.
The shared corpus lock now also covers existing form writers. The source path,
ID, old claims, old creation provenance and unknown companion bytes remain
distinct from the new description and its form bindings. Access remains read-only.

## Risk review

- Source traceability / lineage: yes. Exact old source refs and whole-package
  digests resolve to tracked original byte blobs; current scanners do not
  rediscover archived versions as current identities or provenance events.
- Authored/derived and assessment boundaries: yes. The authored patch and
  reason are retained. Serialization, a source-copy form and a green check do
  not become research assessment, upstream model attestation or admission.
- Scope and identity: yes. An independently selected protected configuration
  limits fields, subject and form IDs. Identity, visibility, rights, claims and
  assessment decisions are not mutable through this adapter. Unknown values
  outside the explicit patch and unselected companion bytes are preserved.
- Related changes / recovery: yes within the declared flat package and
  cooperating local writers. Tests exercise actual process exit before and
  after exchange, replay, revocation, competing record/form writers, archive
  corruption and exact prior-version inspection. The old copy is removed from
  invocation-local staging only after independently verifying its archive.
- Snapshot limit: directory namespace exchange does not make an arbitrary
  multi-file reader traversal transactional. Existing source/form snapshot
  checks detect observed source drift; graph/catalog builds remain separate.
- Other checklist subjects (canon, lived witness, translation, counterpart,
  branch formation, calibration): not applicable; none is changed or admitted.

## Verification and limits

Test-first baseline: both initial revision tests failed because the command
configuration/operation did not exist. The implemented focused route passes
11 tests, including the actual existing catalog → graph → form reader. Existing
source-command regression passes 25 tests; the combined 35-test run before the
last graph integration test passed in 25.484 seconds. Topology discovery passes
26 tests in 4.580 seconds. Source-home validation and `git diff --check` pass.
The final full Growth suite passes 101 tests in 8.330 seconds. Those are local
checks over synthetic semantics and real command/filesystem mechanisms, not CI.

No real historical record was revised in the implementation checkpoint above.
No full-corpus migration, all-profile writer, cross-subject transaction, CI,
merge, public deployment or runtime acceptance is claimed. Full foundation
validation has an already recorded unrelated private-payload boundary; it is
not bypassed here. The active full foundation goal remains open.

Rollback uses a further source-version correction or a derived-reader rollback,
never deletion or relabeling of the committed history. Abrupt-loss staging stays
outside scanners for explicit owner recovery. Package/history byte ceilings are
safety bounds, not host capacity reservations or performance budgets.

## Subsequent actual source correction

After implementation commit `22c61e1d639356db26433144c05cb59ff28896f5`
was checkpoint-reviewed, the same CLI revised
`tos.historical-event.friedrich-nietzsche.jenseits-1886-commission` at
`2026-09-07T05:53:29.665099+00:00` under the explicit full-foundation source
research/correction assignment. This is local-account execution by
`model:codex`, not independent model invocation attestation or admission.

The name now explicitly calls the episode reported. The description preserves
Sommer's attribution and adds the distinction established in the separate
letter-705 reading: the inspected letter requests accelerated printing, does
not name the book, and must not be quoted as Sommer's wording of commencement.
The archive/source research route is named without inventing independent
testimonies or declaring full palaeographic verification. The Russian authored
name and note explicitly declare `ru`/`Cyrl`, not an original-language or
translation claim about the German letter. No initial historical Claim, maker,
provenance or creation receipt was rewritten.

Exact source refs:

- Version 1: `sha256:745dba82c441f213fa7b1b858f583faff99cf34b91aa056f80aa5300c5fac14e`.
- Version 2: `sha256:0ba156dfc3354a0d6f9fa4c69085fd771a5fbaa0ff6ae7933009ec440f18a23f`.
- Prior package: `sha256:faec2c379d51938d44a4ea73fea63797a6e029997d976ad4ecefd7e93cc1a717`.

The current source-adjacent `source-revision-history.json` retains the full
request and exact archive locator. Independent reading compared all seven
archived files, 23,631 bytes, against `git show 22c61e1d6:<original-path>` and
verified every byte hash and size. The five unmodified current companions are
also byte-identical to those originals. Two previous forms remain in history;
both current forms advance to version 2. Current package size is 36,330 bytes.
Fresh CLI replay returned `replayed: true` with the original receipt timestamp;
`inspect-version` independently returned version 1 and all seven file bindings.

The owner builders regenerated catalog and graph after capacity-only host
preflight/reservations for their exact existing directories; both reservations
were released after terminal success. Catalog parity and the actual graph
validator pass. Comparing the graph with the prior commit finds exactly one
changed identity node, no added identity, and byte-equivalent JSON values for
all 196 claim traces and 1,327 edges (697 total nodes). The 49 graph tests pass
in 46.046 seconds. No source-history blob is imported as a current record.

A fresh local `ToSAccessCore.discover(root).knowledge_node(id, relation_limit=20)`
returned one match and three relations. Its complete source record equals the
actual v2 record; `select_human_forms(node, 'ru')` returns both exact source texts,
`ru`/`Cyrl`, source version 2, and null admission. Source revision:
`7b93ca52a629e8a8aaaaa73839fb1d54c659916ab7d79e6bae0c3ae0ea9fe9e9`.
One-process timing: cold node 18.791870577 s, warm node 0.000174937 s,
user CPU 18.069053 s, system CPU 0.705082 s, peak RSS 1,192,844 KiB.
These are local observations, not p95, scaling proof or deployed UI acceptance;
cold graph preparation remains a substantial open performance requirement.

The archive/history command is available through the source owner. Read-only
generic graph discovery of record-version history, document/carrier identities,
participant roles, contextual claims, reception and substantive assessment are
still required by the full goal. No CI, merge, publication or deployment was
performed by this source correction.
