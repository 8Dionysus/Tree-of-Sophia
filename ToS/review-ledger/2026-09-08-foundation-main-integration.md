# Foundation integration with current main

Date: 2026-09-08. Foundation source checkpoint:
`d187c3c8ff7a7cb261d0be017c5dcb735643c8aa` (metadata reader at parent
`fda68d00a94411e3719e4d3446de111f03797a84`). Incoming main:
`0e1ff330612d200750911c64a8f07fcf28c599ef`.
Scope: resolve source integration, preserve owner changes and identify the
remaining delivery boundary. This is not Foundation, CI or merge acceptance.

## Source and history review

- Main's TOS-D-0043 retirement and TOS-D-0044 permanent KAG unfreeze remain
  authoritative for those concerns. Retired Spark/legacy files and local
  shared-skill projections are not restored. Profile discovery remains a
  declaration, not proof of installation or model behavior. A duplicate
  profile-routing paragraph introduced by automatic merge was removed.
- Foundation's unlanded lens and registry decision IDs collided with those
  independently landed records. The explicit source-metadata correction uses
  [TOS-D-0058](../../docs/decisions/TOS-D-0058-backend-defined-knowledge-lenses.md)
  and [TOS-D-0059](../../docs/decisions/TOS-D-0059-stable-semantic-interchange-registry.md).
  Each retains its date, posture, rationale and immutable former commit/path;
  active links were migrated and dated reviews received explanatory addenda.
  Neither semantic entity IDs nor accepted main decision IDs changed.
- The source-navigation builder retains text/annotation projection, assessed
  forms, exact Claim and metadata versions, and main's indexed nearest-parent
  lookup. Historical bodies never borrow current forms or use authority.
  Both branches' source/profile/schema and private-candidate guards remain.
- Main's fixture isolation, deep-copied verified graph queries, root pytest
  collection, local feedback/cache boundaries and one-rebuild raw-byte parity
  checks remain. The authored test inventory preserves both branches' entries;
  it does not replace validation-lane command authority.
- Knowledge-assessment ownership and the explicit semantic-registry change
  gate remain. The restored KAG currentness gate is not freeze-only and cannot
  accept ToS source meaning, rights, competence, canon or runtime state.

## Observed verification

Decision generation/parity and record validation passed. The registry gate
against exact incoming main passed as an explicitly permitted first
introduction: all four old registry/contract objects and the older declared
reader are absent there. This is mechanical introduction evidence, not
semantic compatibility approval.

The merged corpus builder passed in 26.749 s, 410.1 MiB peak, no swap. The
complete `source_witness_foundation` lane passed in 72.132 s, 325.8 MiB peak,
no swap on the actual checkout. Nested route cards and mechanics topology
passed. Generated decision, agent-route and documentation companions were
rebuilt from their sources.

The seven-module integration test run reported 156 passing tests and two
failures, both from the stale KAG candidate receipt/currentness binding;
758 subtests passed. No test expectation or gate was relaxed to clear them.

The first complete bibliography/corpus test run reported 107 passes and five
reported failures in two Sign-version tests. Those tests selected any global
RecordVersion; newly supported metadata versions correctly coexist with the
selected Claim. Selectors now use the Sign's exact promotion-basis reference,
retain deduplication/body/status checks, and additionally check both edge
targets. The focused rerun passed both tests and all six subtests. The later
complete rerun belongs to the commit-bound checkpoint, not this pre-run note.

## KAG delivery boundary

The pinned `aoa-kag@14ee1e33e43749d23c557b3ef526eca7edb36196` full read-only
generation check, with base/history/event all bound to incoming main, stopped
before writing: the observed candidate required 63,349,245 bytes against the
50,331,648-byte global owner ceiling. It ran in 31.486 s, 879.7 MiB peak, no
swap. This measurement precedes the final integration review/selector edits;
the exact final candidate must be checked again.

A budget receipt does not waive the global ceiling. The pinned generator
checks it before the v4 branch, and v4 corpus/distribution schemas also retain
the same ceiling. ToS's current provider validator accepts v3. The separately
verified remote `aoa-kag` main at
`c98250c457e2efe35e93ac1d621568a56afa9dcb` still retains that ceiling, so merely
updating the provider pin is not a demonstrated solution.

Next owner: an explicitly authorized `aoa-kag` corpus/distribution contract
change, followed by coordinated ToS consumer/pin migration and real family
checks. Do not remove required source coverage, alter a generated manifest,
refreeze KAG, or raise a local constant just to obtain green CI. No such
cross-owner source change, budget admission or downstream activation occurred
in this integration.

## UI and assessment boundaries

The independent immutable `fda68d00` UI canary passed 17 transport/model checks
and retained exact event v1/v2 and native Agent history. Browser reading failed
two concrete contracts: full essential record context was not rendered, and
EN fallback discarded the current event version's declared Russian language.
The UI owner has the bounded repair; transport success is not visible-reading
acceptance. Its cold 28.492 s observation is not a performance budget.

The reviewed local source-observation case design is preserved under `evals/`.
It is not a runnable suite, completed calibration, competence grant or source
admission. Actual assessment and permitted-use evidence remain separate work.

Source-return, authored/derived separation, historical identity provenance,
language separation and owner boundaries were reviewed without fabricated
human review or agent authority. The full release/KAG gate, remote CI, final
merge, release, deployment, runtime health and full Foundation acceptance are
not established. Rollback uses the exact parent commits and preserves source
records, archived versions, historical decisions and private custody.
