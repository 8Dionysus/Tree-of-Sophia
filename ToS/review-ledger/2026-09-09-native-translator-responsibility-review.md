# Native translator responsibility review

Date: 2026-09-09 UTC. Scope: the source-owner change after
`dac5d5bb0c74b7de6e0e096405c613c4bce05444`, not Foundation v1 acceptance.
The primary implementation agent holds the Operator's Foundation direction.
Independent helpers reviewed source attribution and the frozen command,
history, citation and reader boundaries; neither supplied a human signature.

## Source and judgment

The [Gutenberg ebook catalog](https://www.gutenberg.org/ebooks/4363), its
[linked author page](https://www.gutenberg.org/ebooks/author/1466) and the
[opening attribution and transcriber note](https://www.gutenberg.org/cache/epub/4363/pg4363-images.html)
were inspected. They are three pages from one provider, not three independent
witnesses. The inspected material attributes the underlying English translation
to Helen Zimmern and reports an antecedent Complete Works source; electronic
typographic, footnote and spelling adaptations remain separately qualified.
This was not a full-text comparison, independent biography, printing
identification, translation-quality assessment, equivalence decision or rights
clearance. English/Latin labels describe the supplied written names, not the
person's native language.

The new [Agent](../source-witnesses/agents/helen-zimmern/agent.json) retains
provisional identity and `no_equivalence_claim`. Its three explicit source-copy
forms expose the supplied name, catalog variant and Russian qualified note.
The [translated_by Claim](../source-witnesses/relations/jenseits-zimmern-gutenberg-4363-translator-attribution/source-claims.jsonl)
remains unreviewed with no assessments. Its Russian statement and
`attribution_scope` retain the source and uncertainty limits above. Creation
and serialization provenance do not certify upstream source reading or give
the Claim semantic admission.

## Owner and boundary checklist

- **Yes — source return and distinct layers.** Exact current metadata owns
  meaning; transaction buffers, receipt, history and catalog remain subordinate.
  The Agent is a person identity, while translator is the Claim's contextual
  role. No Translator subclass or second semantic database was introduced.
- **Yes — restricted change and plurality.** The Expression advances v1 to v2
  and appends one responsibility Claim ref. Other fields and prior ref order
  remain unchanged. A separate relation home keeps the initial Work/Expression
  Claim stream and immutable creation capture intact. Competing attributions
  remain possible; Claim identity is not endpoint-pair uniqueness.
- **Yes — evidence does not manufacture authority.** The three literal URLs
  are attribution evidence addresses. Exact Expression and Agent bytes are
  separately bound read dependencies, not additional historical attestations.
  Graph citation occurrences return to their local citing Claim and expressly
  carry no fetched remote-content hash or observation claim.
- **Yes — source history, recovery and qualification.** Continuous committed
  lineage is required for retained Expression and Agent bytes. Orphan archives
  and ID-only matches are insufficient. Replaying the original attachment after
  a separately authorized descriptive Claim correction verifies the current
  successor; it does not turn the initial receipt into review of new wording.
  Corrections must retain the required statement language, script and scope.
- **Yes — access and authority.** Protected, expiring source grants remain
  distinct from requests and from read-only access. Shared lifecycle extraction
  does not widen old flat-package grants. Rights, consent, publication and canon
  retain their actual owners; no admission is granted by these operations.
- **Not applicable.** No concept merge/split, canon, counterpart, compost,
  calibration, lived witness or philosophical template transition is performed.

## Real execution and retained evidence

The ordinary `source.create` command created Helen Zimmern under an exact
protected grant. It completed in 9.432 seconds, peak 72.1 MiB, zero swap.
Its canonical Agent record digest is
`sha256:ac8695fc9929ec3bcf793c1747bef271725d0851da996e2873a81b7ecee956f1`.
This legacy create route does not participate in the selected-metadata
publication token; catalog regeneration was required before the attachment.

`expression.responsibility.attach` then completed in 1.304 seconds, peak
30.9 MiB, zero swap. Its [receipt](../source-witnesses/relations/jenseits-zimmern-gutenberg-4363-translator-attribution/responsibility-attachment-receipt.json)
binds the exact captured request, Agent, Expression predecessor, qualified
Claim and four explicit forms. Transaction:
`sha256:4b0caf7c1e0b0d29d054a607807359f504b65d8f4960dac59eb9b5fa12bd7b98`;
manifest:
`sha256:f5d17dfa525350e9b551adb915c45505c3178815997550121793d2b31a822a72`.
The selected publication is ready/committed at generation 4, token
`sha256:2ae4daa1ed6cea794f00bf62d95d8412d39b12c226c0140781f0e09f42696d38`.

Work bytes, the Expression's original Work/Expression Claim, receipt and
creation provenance, and all three legacy bibliographic topology streams plus
their provenance preserve their before-operation SHA-256 hashes. Only the
selected Expression record, its rebound forms and history changed in that home.

The source catalog, bibliographic graph and corpus index were rebuilt by their
owners in 50.979 seconds, peak 389.9 MiB, zero swap. A fresh read-only canary
verified both old and new compound receipts, exact Expression v1 historical
bytes, exact Agent bytes, the ready Claim statement and its complete nested
context. It passed in 1.538 seconds, peak 21.7 MiB, zero swap. Two earlier
canary harness runs failed in reporting code after their source assertions;
they are not counted as successful end-to-end runs.

Both commands were retried from their retained original requests using current
grants. Both returned `replayed: true`; publication stayed at the same token and
generation. The combined retry took 1.583 seconds, peak 27.4 MiB, zero swap.

## Implementation checks and remaining scope

The frozen focused run passed 68 test methods and 499 subtests; an additional
inventory subtest initially failed because its failure route lacked the
required leading `Fix`. After correcting that documentation field, the targeted
repeat passed 8 tests and 227 subtests. An intervening wrong pytest selector
collected no tests and is not a passing check. Independent code review found
and verified fixes for corrected-Claim replay, missing qualification during
Claim revision, and the revision dependency binding. No actionable finding
remained at the frozen handoff. The real pre-refactor Work/Expression receipt
also independently verified without changing its six source/capture files.

The independent real source-foundation validator passed in 33.270 seconds,
peak 144 MiB, zero swap. Semantic registry transition validation passed against
the exact pre-change commit above. All remaining eleven steps in the manifest's
source-foundation sequence and its three source graph-export checks passed
together in 107.061 seconds, peak 602.1 MiB, zero swap. No wider regression
run is implied. Local payloads remain optional; checking present bytes does
not establish completeness of unavailable private payloads.

The first foundation-lane attempt exposed an already-stale discovery queue:
its source snapshot still named the Work catalog before the earlier native
Work update. An independent comparison showed identical candidate rows,
counts and next candidate; only the Work-catalog source hash and queue digest
changed. The owning queue builder refreshed that companion, followed by the
corpus index; no candidate judgment or priority was edited. The checks above
include this corrected currentness state.

A separate browser/agent canary consumed this native source with integration
access commit `2b4fca25c0939fc371254b4b56f658fe4139105d`. Full-name and
Helen Zimmern searches, selection of the new Claim, its supplied statement and
complete attribution scope worked; unreviewed status remained explicit.
Repeated search kept its panel open, and language fetch retained the previous
readable heading. Browser warning/error logs were empty. UI acceptance remains
open: numeric search still ranks repository carriers above the named material,
two carriers of one entity are not grouped for human reading, the source-claims
carrier is not a valid dossier handle, and mandatory context is still exposed
as large raw JSON blocks in the main reading surface. The direct canonical
Expression dossier endpoint returned the source packet; this is a consumer
routing defect, not missing source material. The integration owner received
the findings. The test server was stopped after 216.944 seconds, total CPU
44.404 seconds, measured peak 2274840576 bytes, zero swap. This was not a
comparative latency or frame-performance benchmark.

This slice adds one native translator-responsibility operation, not every
bibliographic responsibility or compound growth command. Other predicates need
their actual owner type matrix and grant. Grant-free command discovery,
Edition/Item growth and non-source-copy parent form rebinding remain separate
work. Exact commit-bound checkpoint review and delivery are separate from
these local checks; the integration owner retains the open UI findings. This
note does not claim remote CI, merge, deployment, translation acceptance or
completion of Foundation v1.
