# Real confidential Occurrence growth and historical replay

Date: 2026-09-08. Source-owner agent review within the continuing Foundation v1
operator mandate. This note is a disclosure-reviewed mechanics/source return,
not a linguistic quality verdict, research admission or publication approval.

## Real source growth

The unchanged retained DTA machine layer and opening-sentence proposal named
in [the native return review](2026-09-08-native-text-return-review.md) supplied
the exact source closure. Its current recorded derivative posture supports
local research, while source visibility and publication remain restricted.
No source-rights record was changed or treated as a fresh legal clearance.

A dedicated mode-0700 ToS-owned confidential source tree was selected outside
the public checkout under TOS-D-0055. Its protected explicit context points to
the existing checkout for schemas and to one opaque private namespace for new
sources. It is durable authored material, not scratch, a runtime service,
another ontology, a private copy of public schemas or a public catalog. No
paused stack deployment, service, mount, backup or transfer was activated.

The ordinary source commands created one new native packet with two separately
identified exact surface-span proposals and three explicit complement gaps
within the existing sentence scope. The two selected strings are equal; their
native identities and situated uses remain distinct. This is a deliberately
partial selection, not complete tokenization, source correction, a lexeme or
an accepted semantic interpretation.

Two `local_only` Occurrence descriptions use those complete native bindings
and the existing semantic profile. Each has separate Russian source-copy name
and hover forms, four forms in total, with full scope and source qualifications.
The native language remains German. Byte verification proves the selected
retained representation, not correctness of its transcription or source text.
Native statuses remain proposed; descriptions retain provisional identity;
all creation receipts have `grants_admission: false`.

Ordinary retained record revisions then clarified both descriptions: the
selected fragment and unit boundaries are proposals, not an accepted sentence
analysis. Both records and all four current forms now have version 2. Exact
version-1 inspection succeeds; native bindings, semantic scope, identity and
visibility are unchanged. Replaying both revisions returns their original
receipts without another version increment or an assessment.

Requests, owner configurations, source/native records, forms, environment,
provenance and immutable receipts stay in the protected source tree. This
public note intentionally omits the word, source selectors, short-span hashes,
private joins and store locators. Source text was neither changed nor copied
to a public artifact. No automatic backup is claimed; the source owner must
retain the private tree and its original source dependencies independently of
reader rollback and ordinary task-scratch closeout.

## The real canary changed the next action

Initial native creation and both Occurrence creations succeeded. Repeating the
original native command afterward failed because the digest of the whole
identity inventory had changed. The unrelated source growth did not change the
selected input or native output. A synthetic cross-adapter reproduction then
failed all four historical routes: native creation, source creation, record
revision and form application. Their original receipts and package bytes were
otherwise intact.

The correction belongs to the existing confidential writer contract. Whole
expected dependencies remain mandatory for a new commit. Historical replay
instead binds the original request, receipt and retained exact output, freshly
checks current delegation, native source, rights, grammar and collisions, and
compares two current snapshots before returning. It returns the same receipt,
not a new commit or a claim that every unpinned old input byte stayed unchanged.
Compatible unpinned manifest metadata, harmless grammar-byte changes and
implementation updates are therefore distinguished from a changed exact native
binding or invalid current semantics. No v1 source or receipt was migrated,
rewritten or supplemented with invented historical fixity.

The original real native request and both original Occurrence requests now
replay successfully with their unchanged receipts. The disclosure ceiling and
absence of admission survive. This is real old-receipt compatibility across
the implementation correction, not only a fixture built by the new writer.

An independent review also found that private source forms used a warmed
runtime-global schema cache instead of the selected context's current grammar.
Two semantic schema restrictions were ignored in the reproduced negative test.
Private history, creation/revision and materialization now share freshly read
protected schemas through the same pure compiler as v4 assessment. Public
default readers and their visibility guards were not relaxed.

Negative probes also exercised access revocation during the final dependency
read and response materialization. Replay now assembles the response first,
then checks current dependencies and retained package bytes, then the protected
context and delegation. No new source reading happens after those final checks.
This closes the observed stale-permission return; it is not a transaction over
noncooperating external source edits after the last source snapshot.

## Real v4 inspection, not an assessment

The two native units, two descriptions and four current forms were separately
selected through protected v4 configurations. Both metadata-only and exact
owner-local modes ran `describe` followed by `inspect` for all eight subjects:
32 commands in total. Every result retained `local_only`, no publication,
empty history and `can_use: false`. Exact mode reported verified native text;
metadata mode did not. The result checks rejected private source paths and
short-span hashes in the discoverable command context.

The initial runs took 24.417305 s for metadata-only and 24.821902 s for exact
mode, including local configuration and 16 commands each. These are bounded
observations on the shared host, not accepted latency budgets or corpus/UI
scaling measurements.

After the real revisions, both historical read-mode configurations refused the
changed subject with `JournalConflict: expected subject or owner scope is
stale`. A separately retained generation-2 configuration passed the same 32
commands against the revised descriptions/forms and unchanged native units:
26.827989 s metadata-only and 28.125165 s exact. No earlier configuration,
history, source receipt or assessment was rewritten to hide the change.

No competence, authority grant, model identity or execution calibration was
fabricated to turn these inspections into an assessment. Substantive quality,
independent source-visible review and policy-qualified research admission
remain outstanding. The original native review fields were not relabeled.

## Verification route and limits

The focused growth-across-retry test moved from four errors to a passing root
run (39.709 s); the fresh form-grammar negative moved from two failures to a
passing root run (3.911 s). Final root suites passed: native writer 25 tests
(24.551 s), private writer 29 (347.851 s), v4 assessment 21, native assessment
18, assessment core 62, public source commands 39, human forms 22 and topology
16. The final expanded tests cover late delegation/context drift and access
revocation during response assembly for creation, revision and form replay.
Owner-generated corpus and documentation companions follow their normal
rebuild/check/validate route before this checkpoint is committed.

```bash
python -m unittest discover -s mechanics/growth-cycle/tests -p test_source_text_unit_commands.py -v
python -m unittest discover -s mechanics/growth-cycle/tests -p test_source_owner_profile_commands.py -v
python -m unittest discover -s mechanics/growth-cycle/tests -p test_owner_local_assessment.py -v
python -m unittest discover -s mechanics/growth-cycle/tests -p test_source_commands.py -v
python -m unittest discover -s mechanics/growth-cycle/tests -p test_human_forms.py -v
```

Checklist: source return, separate identity/layers, qualified multilingual
forms, preserved history, explicit rights/competence limits and ToS/AoA
ownership are retained. Public disclosure was reviewed independently from
local source reading. Canon, lived witness, public golden examples and runtime
deployment are not part of this slice. No second schema, identity registry or
historical authority was introduced.

Next source owner: private Claim grounding/creation and qualified assessment,
followed by the remaining language/text profiles and actual consumer routes.
L02 and Foundation v1 remain incomplete. Public graph/UI, Cloudflare/D1, CI,
merge, deployment and published-runtime health are not established here.
