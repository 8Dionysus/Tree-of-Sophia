# Native Collection creation and membership growth review

Review date: 2026-09-09. Baseline:
`85eff02a18db18bece04c5f32f85c34018ba7a91`. Review scope is the local
Collection/typed-descriptor implementation, not a new historical witness,
deployment or acceptance of any membership claim.

## Owned change and boundary review

- Yes: Corpus creation grant v2 adds an empty provisional Collection; v1
  remains closed. A new Collection cannot smuggle membership, publication,
  reviewed identity, supersession or equivalence through descriptive fields.
- Yes: `collection.work.attach` is an independent fixed handler over one
  existing Collection and Work, a new qualified Claim and exact form IDs.
  Only the Collection version/membership refs/forms/history and separate Claim
  package change; the Work and unrelated sibling/descendant bytes do not.
- Yes: retained legacy provenance and verified native compound transactions
  provide the current membership union. Exact closure does not decide
  completeness, truth or philosophical significance.
- Yes: competing membership accounts remain distinct. Claim correction must
  retain its real compound origin, existing endpoint IDs, qualification and
  continuous predecessor stream. Evidence URLs remain address declarations,
  not fetched content or independent source-reading evidence.
- Yes: selected publication preserves exact rollback/resume and current
  authority/dependency checks, including hostile third-state bytes and retries
  after Work/Claim corrections. Metadata readers reconstruct past Collection
  versions through the same continuous selected history.
- Yes: the shared typed descriptor now returns the exact registry type ID.
  `sense` maps to `tos.entity.lexical-sense`; native kinds require one actual
  owner-registry mapping instead of deriving type authority from an ID prefix.
- Yes: source, Claim, Form, assessment, rights, canon and publication remain
  distinct. No source content, operator grant, payload or assessment was
  created by this review. Runtime, proof and memory ownership did not move.

Golden-kernel, lived-witness, multilingual-node, practice-lineage, calibration,
compost and public-mirror changes are not applicable. No new durable decision
or separate candidate registry is warranted by this bounded implementation.

## Verification evidence and limits

The focused Collection, typed mapping, explicit creation-grant, discovery and
script/test topology selection passed **34 tests and 829 subtests**. Native
origin forgery, interrupted publication, explicit rollback/resume, dependency
drift, changed Work/Claim history and old-grant refusal were covered with
synthetic metadata only. Earlier test failures were resolved: the synthetic
sense packet lacked its type-specific semantic fields; one crash fixture
needed to reach a newly created file; shared Claim correction needed the exact
native membership external-citation route. No failure was waived.

The current repository source-witness foundation passed. The explicit registry
transition first refused the missing version advance; relation registry
version 34 records the new profile. The exact pre-change baseline is retained
above. This checks mechanical compatibility, not relation truth.
The explicit transition rerun passed after that version advance.

A broader compatibility run of ordinary creation, metadata/native readers,
existing responsibility attachment and topology is still running at this
checkpoint. Its result is not included in the passed counts above.

No CI, merge, publication, deployment, real-language assessment or corpus
acceptance is claimed. The integration owner receives the checked local
commit and owns cross-branch acceptance. Native Artifact and object-Link
creation remain subsequent separately bounded work.
