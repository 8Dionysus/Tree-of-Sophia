# Declared Claim statement forms — 2026-09-07

Implementation and source-copy review, not substantive acceptance. Parent
source state: `d6a30deb1f53ae799a33d05ba03bd2e24e60d501`; Foundation v1 remains
incomplete. Current law is in `ToS/doctrine/HUMAN_FORMS.md`; command authority
is the explicit source-owner delegation described in the Growth Cycle part.

## Changed owner surfaces

- Shared source-form adapter: complete `qualifiers.statement`, declared
  language/script, and the entire exact Claim as mandatory reading context.
- Existing Growth entrypoint: separate `tos_local_claim_form_owner_v1`
  configuration for one Claim, common discovery/preparation/apply operations,
  exact predecessor history, bounded source/profile/schema reading and receipt.
- Form-set schema: optional source-contract digest map, without rewriting
  historical receipts or granting assessment authority.
- Bibliographic graph and Python/Worker delivery: forms on the Claim identity,
  never its subject/object; ambiguity between source families is refused.
- Initial Claim creation retry: known original-ID form descendants and empty
  locks are allowed while the original five-file receipt stays exact.

No new predicate or runtime service was introduced. Access remains read-only.
The selected statement field is a shared convention of this explicit adapter,
not a predicate-specific Python branch or a universal summary generator.

## Real source use

Source package: `ToS/source-witnesses/relations/nietzsche-letter-705/`.
Three version-1 forms were created with the common protected source command,
and exact retries returned their retained receipts:

| Claim suffix after `tos.claim.` | Form suffix after `tos.form.` |
| --- | --- |
| `nietzsche-letter-705.sender` | `nietzsche-letter-705.sender.statement-ru` |
| `jenseits-1886-commission.letter-705` | `jenseits-1886-commission.letter-705.statement-ru` |
| `nietzsche-letter-705.concerns-jenseits` | `nietzsche-letter-705.concerns-jenseits.statement-ru` |

Each is a whole-field Russian source-copy, script unknown, `ready` only as a
rendering, `standalone_reading: false`, `admission: null`. Eight exact source
contract inputs are retained in each command receipt. Private delegation files
remain outside the public source tree; receipts authenticate a local account,
not an independently attested model invocation.

The five initial Claim-package files were compared against the parent commit
and remained byte-identical. Initial creation replay still returned the same
receipt after form creation. All 721 graph node IDs, 1344 edges and 199 Claim
traces were unchanged; only the three Claim nodes gained forms/source returns.
The actual `ToSAccessCore` selected all three statements and focused each Claim
(6, 7 and 7 nodes respectively). This is source access, not semantic admission.

## Verification and findings

- Test-first command case initially failed on unsupported Claim-form scope;
  implementation then passed creation, revision, exact retry and CLI replay.
- Negative controls reject omitted context, wrong subject/creator/role/language,
  unknown or private source, duplicate selected ID, bad path and stale schema.
  Source changes make old materializations stale without erasing receipts.
- Pure form checks retain false/null/zero/empty/unknown instruction-like data,
  refuse freeform and access widening, and return over-budget without truncation.
- Growth Cycle: 121 tests passed, 47.207 s on this local run.
- Bibliographic graph: 58 tests passed, 58.238 s. The first full run exposed a
  pre-existing test assumption of two fixity files. The earlier declared-source
  assessment adapter now binds four additional schema/registry files. The test
  was corrected to compare the full path set and every digest, not weaken it
  to an arbitrary new count. Private metadata is now refused earlier by its
  profile; the test accepts that explicit refusal route too.
- Access knowledge contract: 51 tests passed, 21.258 s.
- Worker knowledge suite: 10 tests passed, including Python/pure TypeScript/
  local Miniflare D1 parity for compact delivery of the actual Claim wording.
- Worker TypeScript typecheck passed. Script/test topology: 26 tests passed.
- Source-home validation, catalog currentness, graph rebuild/validation and
  whitespace checks passed. Corpus index is regenerated as the documentation
  companion, not as evidence of semantic acceptance.

## Boundary review and recovery

Source traceability, Claim/object separation, full qualifications, language
unknownness, retained lineage, and ToS/access/assessment boundaries: yes.
Canon, private payload, witness text, rights, consent and publication changes:
not applicable; none performed. No source statement was rewritten or approved.

A reader rollback leaves new form sets and growth history intact; older closed
form-set schemas must reject the optional new receipt member until updated,
not strip it. Do not reset the Claim source or erase new history to roll back
a projection. Lost-response retry and stale-source behavior use the existing
atomic form-set boundary and shared account locks, not a multi-file snapshot.

Legacy Claim streams, assessed freeform/templates, generic Claim correction,
full profile coverage, UI gestures, scale budgets and substantive agent review
remain open in the Foundation map. No CI, merge, release, remote D1 deployment
or published runtime claim is made by these local tests. There is no new memo,
eval or promotion candidate: the reusable operation is in its existing owner.
