# Historical German Morphology B Artifact Admission — 2026-08-10

## Relation to the frozen research packet

This is an additive result packet. It does not rewrite
`HISTORICAL_GERMAN_MORPHOLOGY_ADMISSION.md`, whose exact digest is bound into
the output-blind contextual B plan. That older packet proves that the question,
selection, artifact identity, and stop conditions existed before B output.

The exact `de_zdl_lg 4.0.0` wheel and a CPython 3.12 dependency closure were
acquired into owner-local private cache after storage and `medium/indexing`
resource admission. The freezer verified 41 reachable wheels, the principal
627,548,130-byte wheel digest, the source commit, compatible runtime tags, and
a deterministic requirements lock. The acquisition took 356.746 seconds; its
owner receipt reports a 2.1G memory peak and zero swap. One earlier downloader
attempt failed before writing wheels because its interpreter had no `pip`; that
negative attempt remains retained.

## Artifact and rights boundary

Acquisition did not imply executable trust. The source repository declares
GPL-3.0-only, while the principal wheel and its model metadata contain no model
license and no single embedded training-source license. The private bundle
therefore remains non-redistributable and requires manual license review. ABI,
SBOM, ML-BOM, and SLSA/in-toto sidecars were produced, but the required Cosign
control was unavailable. No signing key was generated to manufacture a green
result. The registry record remains a non-latest `candidate` under a
`local_dev` trust root, its exact subject store is incomplete, verification is
false, and verified controls are empty.

The identity-bound runtime trust gate returned `deny`: there was no eligible
latest record; verification errors and missing required evidence remained;
required controls were not verified; and a production runtime consumer
requires a non-local trust root and release lifecycle. The source- and
path-free refusal is recorded in
`morphology-contextual-episode.selected-form-b.artifact-admission.v1.json`
with a separate provenance event.

## Result and continuation law

No ZDL runtime was built, the frozen three-row source packet was not consumed,
and B produced no output. Quality and execution speed therefore remain
unmeasured; acquisition cost is not model performance. No accepted German,
morphology, lemma, lexeme, sign, semantic, graph, canon, publication, or human
state changed.

At that snapshot, the recorded continuation was to close the model-license,
signature, trust-root, lifecycle, verification, and subject-store requirements
and rerun the same gate. B could not be bypassed. The additive 2026-08-12
section below narrows the active builder consumer without rewriting this
historical production-runtime verdict. C remains blocked until a distinct
normalization question exists.

## Additive executable-stop refresh — 2026-08-12

The historical `runtime` refusal above remains unchanged. The later
`HISTORICAL_GERMAN_MORPHOLOGY_B_EXECUTABLE_STOP_LINE_2026-08-12.md` return
corrects the active pre-build consumer boundary: the laboratory builder asks
the current exact `agent` gate before acquisition verification or runtime-root
creation. Production-only trust-root and release-lifecycle reviews therefore
remain separate, while latest-record, verification, Cosign/control, and
subject-store closure still deny the builder. Reviewed `abyss-stack`
checkpoint `87d85ad1` makes that stop executable and a real final invocation
left the runtime absent without consuming the source packet. Rights remain a
separate private-only, no-redistribution gate; B and all downstream authority
remain unchanged.
