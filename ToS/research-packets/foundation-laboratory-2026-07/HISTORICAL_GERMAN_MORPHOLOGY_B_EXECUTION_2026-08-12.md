# Historical German Morphology B Execution — 2026-08-12

## Result

The frozen contextual B question has now executed exactly once as an
owner-local, private, two-pass ZDL episode. The three source contexts selected
before B output were consumed unchanged. Both provider passes produced the
same byte stream. This establishes artifact admission, runtime closure,
source return, repeatability, speed, memory, swap, and byte cost for this
bounded method episode.

It establishes no German correctness. All three target occurrences received
one exact provider-token alignment and the provider proposed `ADV`/`ADV` for
all three, but there is no German-competent gold and no human linguistic
review. The proposals therefore remain unreviewed machine output. Accepted
tokenization, morphology, lemma, lexeme, sign, semantics, graph, canon,
publication, and human-task effects are all zero.

## Additive relation to the negative admission

This result does not rewrite or supersede:

- the output-blind plan and private three-row packet;
- the 2026-08-10 41-wheel acquisition and denied artifact-admission receipt;
- the later executable pre-build stop-line report.

Those records remain true for their exact artifacts and times. The first
negative bundle remains evidence that an acquired model is not executable
merely because its files are present. The later execution uses a different,
exactly identified 42-wheel bundle and a new locally promoted registry record.

## Source-free dependency failure and correction

The first later bundle passed artifact admission but failed its source-free
runtime smoke before any Nietzsche packet was read. spaCy 3.8.11 imported the
external `click` package while the resolved Typer 0.27.1 line no longer
installed that dependency. The failed runtime is preserved outside the active
runtime path.

The freezer now rejects that metadata-complete but import-incomplete graph and
requires:

- `click==8.2.1`;
- `typer==0.24.1`;
- `typer-slim==0.24.0`.

This correction follows the current official packaging evidence: Typer
0.27.1 documents the vendored-Click line, Typer 0.24.1 declares external
Click, Click 8.2.1 is the exact admitted wheel, and the spaCy tracker records
the 8.2.1 compatibility work.

- [Typer 0.27.1](https://pypi.org/project/typer/0.27.1/)
- [Typer 0.24.1](https://pypi.org/project/typer/0.24.1/)
- [Click 8.2.1](https://pypi.org/project/click/8.2.1/)
- [spaCy Click compatibility issue](https://github.com/explosion/spacy/issues/13879)

This is an import-closure correction, not evidence about model quality or
rights.

## Exact artifact and runtime closure

The executed route is bound to:

- 42-wheel acquisition set
  `0e9394b4eb8bf78449dc8e991fa61fa37efc8bd34576515b8e675fd7e3b482e7`;
- lock digest
  `e3e16222815b7b471144930274d46e7b118c083458d1c02998dd46a15b3adb2b`;
- signed subject
  `sha256:9768ec14c9f4e0115bf285b7e50f24521cecbd7533c20fb00e0bb7204aba9ff4`;
- 44-file subject-store aggregate
  `sha256:df79c75c0e521bc5f1b379d7b06f27d7d433b5b8bac048fa5ed0ff088d48eed2`;
- local registry record
  `sha256:d6356a650a3f033469759a380264cbe00c1401b5713f90b97df674e04363a80b`;
- all five required, present, and verified controls: ABI signature, SBOM,
  ML-BOM, SLSA/in-toto, and Sigstore/Cosign;
- latest-eligible, manually verified, `local_dev` lifecycle evidence;
- exact `agent` trust-gate verdict `allow`;
- runtime inventory digest
  `591824890eefdbc9f2d79897640a47fdd5c128b87f7eb29e70203f5dda97f561`.

The isolated Python 3.12 runtime contains 977,683,987 bytes. `pip check`
reported no broken requirements. A source-free smoke loaded ZDL 4.0.0, spaCy
3.8.11, and the six-component pipeline before source use.

Artifact trust has no rights effect. The model wheel and heterogeneous
training-source license posture remain `NOASSERTION` at the executable bundle
layer, so the runtime is admitted only for bounded local private research.

## Preflight and execution

The dedicated B preflight rechecked the exact plan, private packet digest,
runtime manifest, host resource admission, and storage owner. A verified
runtime removes already materialized download bytes from the pending storage
request; only bounded output bytes are requested. The static memory floor is
6 GiB, while fresh `abyss-machine` admission remains authoritative.

The executed private run is
`tos-historical-german-morphology-v1/zarathustra-zdl-b-20260812t1354z/variant-B`.
It is mode-`0600`, retained, network-free, and
`awaiting-triggered-review`. No review is automatically scheduled.

Measured result:

| Dimension | Observation |
| --- | --- |
| frozen source rows | 3 at recurrence ranks 1, 73, 145 across parts 1, 3, 4 |
| exact single-token alignments | 3 |
| split or expanded alignments | 0 |
| pass-1 stream | `cf32d4b2730675241fff501d851aa95a7eae9eeedfcc67819f1a5fc39e9407a1` |
| pass-2 stream | same digest; 0 mismatches |
| pass-1 wall / CPU | 0.098123434 s / 0.088108849 s |
| pass-2 wall / CPU | 0.084551051 s / 0.084010459 s |
| pass-1 throughput | 30.573736 contexts/s |
| process peak RSS | 1,273,647,104 bytes |
| owner service wall / CPU | 11.118 s / 9.935 s |
| owner observed footprint peak | 1,892.715 MiB |
| owner observed swap peak | 0.25 MiB |
| raw private output | 39,869 bytes |
| private source packet | 5,203 bytes |
| tracked quality status | `unmeasured-no-german-competent-gold` |

The roughly 0.1-second provider pass must not be presented as the whole
operational cost. The owner service time includes startup and governed
execution. Artifact acquisition and runtime build are separately retained
setup costs.

## Tree of Sophia return route

Reviewed `abyss-stack` checkpoint `a321401b` owns the import-closure guard,
dedicated preflight, runtime execution, and private receipts. Tree of Sophia
adds a separate result contract and recorder. The recorder rereads every
private output row, reconstructs the unchanged token stream, verifies exact
target overlap, recomputes only text-free aggregates, and rechecks:

- the frozen plan and context receipt;
- the retained negative admission;
- private packet mode and digest;
- run, metrics, execution, repeat, runtime, trust-registry, and resource
  evidence;
- source withholding and zero downstream authority.

The tracked receipt and provenance include no source string, provider lemma,
context sequence, occurrence identifier, source offset, `local-content` path,
or absolute `/srv` path. The private inputs and outputs are not published.

## Continuation law

This episode is closed as a machine proposal awaiting a real source-grounded
trigger. It does not create review debt. Do not ask a human to repeat or rate
the run merely because it exists.

If a later accepted source, translation, sign, or interpretive question needs
this exact distinction, the preserved proposal may be surfaced to a
German-competent reviewer under a task-specific blind protocol. Until then:

- provider labels are not gold;
- byte repeatability is not correctness;
- artifact admission is not redistribution permission;
- C remains blocked as question-inapplicable because the exact and normalized
  selected-form identities do not differ;
- no linguistic or semantic fact enters the Tree.
