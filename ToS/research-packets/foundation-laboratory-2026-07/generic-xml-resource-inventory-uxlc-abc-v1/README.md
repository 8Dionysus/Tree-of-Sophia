# Generic XML resource inventory and UXLC projection A/B/C laboratory

This directory executes the immutable
[`GENERIC_XML_RESOURCE_INVENTORY_UXLC_ABC_PREREGISTRATION_2026-08-30.md`](../GENERIC_XML_RESOURCE_INVENTORY_UXLC_ABC_PREREGISTRATION_2026-08-30.md).

It compares:

- A — one opaque XML-document resource;
- B — a generic expanded-name element topology bound to an exact File;
- C — a UXLC-specific primary inventory challenger;
- B+C — B owner bytes plus a separate UXLC provider projection.

The exact UXLC payload and all source-derived candidate outputs remain mode
`0600` below the Git-ignored source payload tree in
`/srv/AbyssOS/Tree-of-Sophia/ToS`. Only public-synthetic outputs, text-free
receipts, research, manual source/output review and the final bounded verdict
belong here.

No file in this directory is an admitted source Item, public contract,
accepted text, word identity, translation, semantic relation, graph state or
publication authorization.

## Completed result

The source-visible comparison is complete at
[`COMPARATIVE_VERDICT.md`](COMPARATIVE_VERDICT.md). After one preserved runner/
lxml-proxy failure and one preserved sealed-arithmetic false failure, all 25
hard gates pass. B is preferred as the generic exact-file XML element owner; A
remains the capture view; C is rejected as a generic owner and retained only as
a derived UXLC provider projection over B.

The public contract, canonical inventory builder and UXLC Item remain
unchanged pending a separate rights/compatibility promotion decision.
