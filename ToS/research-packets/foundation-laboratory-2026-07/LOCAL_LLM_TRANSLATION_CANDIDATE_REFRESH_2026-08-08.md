# Local LLM Translation Candidate Refresh — 2026-08-08

Status: current official and live-host refresh complete; one resident E4B
capacity probe selected; no download, model execution, accepted translation,
human task, semantics, graph truth, publication, or canon effect

Owner boundary: Tree of Sophia owns the admitted source route, experimental
question, blind-comparator law, and any later content judgment. `abyss-stack`
owns the private execution profile and immutable run receipts. `abyss-machine`
owns current model, runtime, storage, memory, load, and thermal admission.

## Decision first

The next useful AI-only episode is one new private Gemma 4 E4B direct-prompt
candidate over the already admitted 20-token `Za-I-Vorrede-1` opening input.
It must use the same direct prompt family, output contract, deterministic
decoding, and sealed-comparator posture as the frozen E2B A result. This makes
the new question narrow: does the larger already resident same-family model
clear the obvious Russian-surface failure that made A unworthy of human
attention?

This is a successor single-candidate episode, not a fourth member retroactively
inserted into the closed A/B/C calibration. The old A/B/C outcomes remain
immutable. The successor profile must bind their relevant digests as baseline
evidence, freeze before the E4B output, and write source-bearing requests and
responses only under the owner-only `/srv/abyss-machine/storage/artifacts/`
route.

The machine may inspect the result only for Russian surface, structural
contract completion, visible uncertainty, and obvious failure. It must reject
an obviously defective result before human review. A result that clears that
floor still remains an unaccepted candidate; German correctness, translation
fidelity, etymology, semantics, and promotion remain unavailable.

## 1. Classical and current primary documentation

Google's current Gemma release ledger, last updated 2026-07-02, still places
Gemma 4 E2B and E4B at 2026-03-31, their MTP assistants at 2026-04-16, and the
12B Unified model at 2026-06-03. No later small Gemma release appears in that
official ledger as of this refresh.

- [Google Gemma releases](https://ai.google.dev/gemma/docs/releases)
- [official Gemma 4 E4B model card](https://huggingface.co/google/gemma-4-E4B-it)

The resident artifact is not the official Google weight file. It is the exact
Unsloth `UD-Q5_K_XL` GGUF at upstream revision
`653803f092503c04a65164346f3208a36e707693`, so every profile and receipt must
name that conversion, its local SHA-256, and the pinned runtime. Official base
model claims cannot stand in for local quantized behavior.

The official TranslateGemma 4B card still describes a specialist translation
model over 55 languages, a 2K input context, and a strict source/target language
code template. The accessible card does not itself establish the exact
German-to-Russian direction for this local episode; the artifact is also absent
and access remains license-gated. It therefore stays a later specialist
challenger rather than displacing a zero-download resident control.

- [official TranslateGemma 4B model card](https://huggingface.co/google/translategemma-4b-it)

The current official Qwen3.5-4B card describes 4B parameters, 201 languages and
dialects, a 262,144-token native context, default thinking, and current
framework requirements. It explicitly recommends at least 128K context for
full thinking behavior. On this host that would be a new artifact, conversion,
runtime, and reduced-context experiment, not a fair zero-cost replacement for
the resident E4B probe.

- [official Qwen3.5-4B model card](https://huggingface.co/Qwen/Qwen3.5-4B)

The upstream `llama.cpp` release stream reached `b10327` on 2026-08-08. The
host's E4B route remains pinned to the already measured `05ff59c` Vulkan build.
Changing the runtime at the same time as model capacity would add an
unnecessary variable. Freshness is therefore recorded, not automatically
promoted.

- [`llama.cpp` releases](https://github.com/ggml-org/llama.cpp/releases)

## 2. Established evaluation basis

The established challenge-set, expert-context, literary-translation, and
human-assistance findings remain those already ordered and cited in
`LOCAL_LLM_ADMISSION.md`. They require a small predeclared question, separate
source comprehension from literary adequacy, keep model suggestions out of an
unassisted human baseline, and measure human correction only when a candidate
is good enough to justify that attention.

This refresh does not replace that literature review. It applies it: one
question, one changed factor, preserved negative evidence, and no LLM judge as
translation authority.

## 3. Fresh official and live-host edge

At `2026-08-08T15:48:33Z`, the host-owned
`abyss-machine ai llm workhorse preflight --json` returned `decision=allow`
with no blocked reasons:

- E4B GGUF present: 6,656,152,736 bytes;
- context: 8,192;
- available memory: 16,100.9 MiB;
- free swap: 7,283.2 MiB;
- swap used: 63.011%;
- memory PSI full/some `avg10`: 0.0 / 0.0.

The current cooling record at `2026-08-08T09:48:22-06:00` reported 94 °C,
below the operator's 105 °C watch boundary and with
`allow_work_with_normal_monitoring`. System load was 5.21 against the existing
laboratory ceiling of 8.0. These are point-in-time readiness facts, not a
standing permission or a quality result.

The same process snapshot showed a separate repository-index generation task
using the machine. The E4B episode should therefore repeat every owner gate
immediately before execution and preserve concurrent-load evidence. It should
not run merely because this earlier snapshot was green.

Authoritative local facts:

```text
/var/lib/abyss-machine/ai/llm/registry/latest.json
/var/lib/abyss-machine/ai/llm/workhorse/gemma4.workhorse/preflight/latest.json
/var/lib/abyss-machine/cooling/latest.json
/var/lib/abyss-machine/processes/latest.json
/etc/abyss-machine/AGENTS.md
/etc/abyss-machine/storage-policy.json
```

## Frozen successor method

The successor episode must:

1. bind the current human-limited eKGWB citation-witness decision and current
   text-free source-return overlay by SHA-256;
2. bind the exact historical E2B A run and its negative ToS receipt by digest;
3. use the resident E4B `UD-Q5_K_XL` artifact without a download;
4. preserve the A direct prompt, output contract, temperature `0.0`, top-p
   `1.0`, seed `42`, and 768-token ceiling;
5. freeze before output and keep all request, response, source, and candidate
   text private;
6. capture fresh storage, memory, load, process, and thermal admission before
   execution and retain the post-run state;
7. verify run fixity independently;
8. triage Russian surface before creating any human task;
9. record quality, speed, token usage, memory, swap, and thermal facts as
   separate dimensions;
10. create no accepted German, translation, etymology, semantic, graph,
    publication, or canon claim.

## Stop line

Do not download Qwen3.5, TranslateGemma, a newer GGUF, or a new runtime for this
episode. Do not rerun historical A/B/C. Do not reveal Antonovsky or another
recognized translation. Do not ask the operator to correct a candidate that
still has obvious Russian defects. A later specialist or different-family
wave is justified only by the exact E4B outcome and a separately refreshed
artifact/runtime/rights admission.
