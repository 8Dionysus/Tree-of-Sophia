# Historical German Morphology B Executable Stop Line — 2026-08-12

## Scope and relation to the earlier receipt

This is an additive cross-owner engineering return. It does not rewrite the
output-blind contextual plan, its private three-row packet, or
`morphology-contextual-episode.selected-form-b.artifact-admission.v1.json`.
That v1 receipt remains the historical 2026-08-10 record of the exact
production-`runtime` trust-gate refusal. The present return records a narrower
fact: `abyss-stack` now enforces the current pre-build artifact decision in the
materializer that could consume the private wheel bundle.

No source text, context, private path, model output, or new model byte is
carried by this packet.

## Correct consumer boundary

The pre-build consumer of the acquired wheel closure is the laboratory
builder/agent, not an already materialized production runtime. The executable
route therefore asks `abyss-machine` for `consumer_intent=agent` while binding
the exact artifact class, registry, record ID, subject digest, source owner,
source ref, trust-root mode, and latest-record requirement.

This distinction removes two production-only manual-review reasons from the
pre-build question: a non-local trust root and release lifecycle remain
requirements for a future production runtime, but they are not the reasons
the current laboratory builder is denied. The candidate still fails closed
because no eligible latest record exists, verification is false and contains
errors, the required Cosign sidecar is absent, and none of the five required
controls is verified. An admitted result would additionally need a complete
subject store.

## Executable boundary in `abyss-stack`

Reviewed `abyss-stack` checkpoint
`87d85ad148b4d7be751912acfafb95a4541e3760` makes the stop line part of the
runtime builder rather than an operator convention:

1. the exact current `agent` trust gate runs before acquisition verification
   and before creation of the runtime root;
2. only `allow` or `warn` with an explicit fail-closed allow decision, exact
   record and subject identity, latest eligible non-terminal lifecycle,
   complete verification, all five required controls, no blockers or manual
   review, matching source/trust-root claims, and a complete subject store may
   continue;
3. the admitted subject aggregate must match the bundle manifest,
   `artifact.subjects.json`, `artifact.verify.json`, the exact acquisition
   receipt, requirements lock, and every acquired wheel;
4. an admitted runtime must retain the gate receipt in its removable inventory,
   and later runtime-integrity checks re-evaluate the full identity and control
   claims instead of trusting only the receipt digest;
5. the ZDL runtime schema and the lightweight admitted-runtime recheck both
   reject a missing artifact-admission record.

The checkpoint review status is `reviewed`. Its deterministic portable KAG
family was regenerated and accepted by the pinned owner validator, but that
derived family is navigation evidence only; the `abyss-stack` commit owns the
runtime behavior.

## Direct proof

The final real materializer invocation used the exact retained private bundle
and registry identities. It exited `1` with the current `agent` blockers and
left the exact runtime target absent. Because the gate precedes acquisition
verification, the frozen Tree source packet was not read or consumed.

Verification against the final bytes produced:

- 9 focused ZDL tests passed, including real-deny ordering, exact command
  binding, subject-inventory drift rejection, missing-admission rejection, and
  semantic rejection of a digest-updated receipt with incomplete controls;
- all 240 Tree of Sophia laboratory tests passed;
- all 1,666 `abyss-stack` tests and 230 subtests passed in an isolated
  Python 3.12 environment matching the repository workflow, with 30 declared
  skips;
- stack validation, nested-agent validation, the source-fast authority gate,
  full and incremental portable-family parity, family-contract validation,
  and reconstruction of all seven compatibility views passed.

These checks prove the stop-line mechanics. They do not prove artifact rights,
model quality, German correctness, morphology, lemma, lexeme, or execution
fitness.

## Rights boundary

The official project source declares GPL-3.0, while the acquired model wheel
and model metadata still do not state one model license and the upstream
training/evaluation datasets have heterogeneous terms. Artifact verification
cannot manufacture permission. The retained bytes therefore remain
owner-local, private-research-only, non-redistributable, and not authorized for
public serving.

Primary owner surfaces rechecked for this return:

- [official ZDL project](https://gitup.uni-potsdam.de/zentrum-lexikographie/spacy-models)
- [official project license](https://gitup.uni-potsdam.de/zentrum-lexikographie/spacy-models/-/blob/main/LICENSE)

## Result and continuation law

The B state remains `artifact-acquired-admission-denied-b-not-run`. No runtime
was built, no source-bearing context was consumed, no model was invoked, and no
B output, quality, speed, or execution-cost result exists. Accepted German,
morphology, lemma, lexeme, sign, semantic, graph, canon, publication, and human
task effects remain zero. C remains question-inapplicable.

A legitimate private laboratory resumption requires a signed and completely
verified exact bundle, latest-eligible durable promotion, a complete subject
store, and a fresh exact `agent` verdict that the materializer itself accepts.
The unresolved model-rights review and private-only boundary remain separate
and cannot be satisfied by a green integrity gate. Any production consumer
would additionally require its own non-local trust-root and release-lifecycle
admission. No direct builder invocation may bypass these routes.

## Later additive outcome on 2026-08-12

The stop line above remains the exact outcome for its denied 41-wheel
candidate. A later, separately identified 42-wheel bundle closed the required
local-development controls, exposed and corrected one source-free
spaCy/Typer/Click import gap, received an exact `agent` allow verdict, and ran
the frozen B packet privately. That later evidence is recorded without
rewriting this document or the negative receipt in
`HISTORICAL_GERMAN_MORPHOLOGY_B_EXECUTION_2026-08-12.md` and the tracked
text-free result receipt. It changes the executable state, not the linguistic
authority: German accuracy, accepted morphology, lemma, lexeme, sign,
semantics, graph, canon, publication, and automatic human work remain zero.
