# Zarathustra Trilingual Entry

This document records the current bounded ToS route for opening manual corpus
movement through *Thus Spoke Zarathustra*.

It now also serves as the first public worked capsule for the ToS tiny-entry
seam.

The point is not to open three language trees at once.
The point is to begin one source-first route that stays bounded, reviewable, and
honest about language roles.

## Tiny-entry role

Within the current public tiny-entry chain, this document is the capsule surface
for:

- `README.md` as the current `tos-root`
- `tree/source/friedrich-nietzsche/thus-spoke-zarathustra/prologue-1/node.json`
  as the canonical authored source node
- `examples/source_node.example.json` as the current public compatibility
  authority surface
- `tree/concept/becoming/node.json` as the canonical authored bounded hop
- `examples/concept_node.example.json` as one bounded public compatibility hop
- `docs/KNOWLEDGE_MODEL.md` as the in-repo fallback orientation surface

This document explains the route.
It does not replace the canonical tree node or the compatibility authority
surface that mirrors it.

## Core move

ToS now begins real tree motion through one bounded trilingual entry route
inside `tos.source.thus-spoke-zarathustra.prologue`.

The current deepening pass keeps the same node identity and the same bounded
concept horizon, but it now opens the first prologue movement through a
paragraph-aligned twelve-segment spine.

The landed bundle therefore now keeps:

- 12 shared paragraph-aligned segments from the first prologue movement
- 1 shared `node_id`
- 3 language witnesses
- 2 concept companions: `tos.concept.becoming` and `tos.concept.overcoming`

This remains a gate-opening route, not wider world-thought expansion.

## Canonical and compatibility posture

The current route now distinguishes two ToS-owned surfaces on purpose:

- `tree/.../node.json` is the canonical authored source node
- `examples/source_node.example.json` is the current public compatibility mirror
  used by the tiny-entry and export seam

This keeps the repository as a real tree-bearing project while preserving the
current public and downstream-safe entry path.

## Language role contract

The current route keeps asymmetric roles on purpose:

- German is the `canonical_source`
- Russian is the `working_translation`
- English is the `bridge_translation`

German remains the authoritative source witness.
Russian and English remain reviewable witness layers around that source rather
than replacement canons.

In the current donor posture, Russian and English are explicitly carried as
Dionysus witnesses inside the paragraph-aligned trilingual surface.
That attribution should stay visible rather than collapse back into anonymous
maintainer text.

## Donor and provenance posture

The bounded route now takes its witness-facing donor from a paragraph-numbered
trilingual markdown surface whose current public labels are:

- German block: facsimile-backed witness from the first volume of 1883, with
  full Nietzsche line-break restoration still pending
- Russian block: translation by Dionysus
- English block: translation by Dionysus

The source file now lives at:

- `sources/friedrich-nietzsche/thus-spoke-zarathustra/prologue-1/Z_1_1_1_de_ru_en.md`

The authority surface should keep this provenance visible.
That means each witness block may expose:

- a `witness_ref`
- an `edition_or_source`
- an optional `translator_or_editor`
- an optional `publication_year`
- an optional `normalization_note`
- shared paragraph `locator` values

The point is not bibliographic maximalism.
The point is to prevent the German source witness and the two Dionysus witness
layers from blurring into anonymous text blocks.

## Paragraph spine and lineation posture

The current route now treats `[1]...[12]` as the shared local locator spine
across `de / ru / en`.

These numbers are not disposable convenience labels.
They are the first public small-scale alignment handle for this route.

The route also treats lineation as meaningful witness data.
That means:

- Russian line breaks now matter as part of the witness surface
- English paragraph numbering is already aligned, but closer line-break
  restoration remains later work
- German paragraph numbering is aligned, but full Nietzsche line-break
  restoration remains later work

The route is allowed to keep paragraph boundaries even where a sentence runs
across the break, because the donor surface itself is now part of the bounded
reading discipline.
In this pass that especially matters for `[5]` and `[6]`.

## Single-tree witness discipline

One meaning should keep one `node_id`.

The current route therefore grows:

- one source node with `language_witnesses`
- 12 shared `segment_id` values across `de / ru / en`
- bounded witness provenance where translator, edition, or donor posture would
  otherwise become ambiguous
- bounded `translation_tensions` only where drift is philosophically
  load-bearing

It does not grow:

- three language-specific node copies
- multilingual relation tables
- language-specific concept nodes

## First bounded route

The first bounded route still uses:

- `tos.source.thus-spoke-zarathustra.prologue`
- `tos.concept.becoming`
- `tos.concept.overcoming`

The twelve-segment spine is now:

- `[1]` leaving homeland and lake
- `[2]` relational address to the sun
- `[3]` cave, eagle, serpent
- `[4]` mutual blessing through overflow
- `[5]` wisdom grown heavy
- `[6]` gift, distribution, and `Thorheit`
- `[7]` katabasis toward sea and underworld
- `[8]` `untergehen` as chosen down-going
- `[9]` the tranquil eye without envy
- `[10]` the overflowing cup and reflected radiance
- `[11]` return to the human
- `[12]` the beginning of Zarathustra's going-under

This is enough to hold solitude, ripening, overflow, blessing, descent, and
return inside one bounded authority surface without widening the branch.

## Return boundary

This pass keeps a strict distinction between return as action and eternal return
as principle.

Inside the current prologue route:

- return is already active as a repeated movement of descent and re-entry into
  the human
- eternal return is not yet introduced as an explicit concept node or doctrinal
  claim

What appears here is a hidden rhythm of recurrence, not yet a separately
stabilized ToS concept surface.

## Mode B deepening posture

The next valid move for this route is now implemented as deepening rather than
widening.

For the current deepening pass, that means:

- keep the same `node_id`
- replace the earlier two-block scaffold with the current 12 shared
  paragraph-aligned segments
- keep `becoming` and `overcoming` as the only concept companions
- sharpen witness provenance now that Russian and English are explicitly
  Dionysus witness layers
- record only translation tensions that remain philosophically load-bearing
  inside this bounded movement

This keeps the first route narrow while making its source surface much more
legible.

## Restartable checkpoint boundary

A restartable checkpoint pack may accompany this route for operational
continuity across bounded inquiry passes.

That pack is an operational sidecar.
It is not ToS canon, not a new node family, and not a substitute for the source
node or review note.

If a later pass stabilizes a genuine authored insight, that insight should move
into an explicit ToS surface under human review rather than remaining implicit
inside the checkpoint pack.

## What remains later

This route does not yet open:

- wider world-thought expansion
- cross-tradition multilingual growth
- separate `context_node` tiny-entry expansion
- a stabilized `eternal_return` concept node for this route
- translation automation

First the paragraph-aligned trilingual route has to hold under review.
See [TINY_ENTRY_ROUTE](TINY_ENTRY_ROUTE.md) for the tree-first seam that now
wraps this worked route.
