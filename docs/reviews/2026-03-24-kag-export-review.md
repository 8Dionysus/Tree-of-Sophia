# 2026-03-24 KAG Export Review

## What changed

- added a source-owned tiny KAG export posture note in `docs/KAG_EXPORT.md`
- added `scripts/generate_kag_export.py` and `scripts/validate_kag_export.py`
- added generated `kag_export` outputs for the current Zarathustra prologue route
- updated `README.md` so the export posture is visible as a bounded downstream surface

## Most at-risk checklist items

- downstream consumption being mistaken for downstream authority
- the authority source node being displaced by a derived export capsule
- the tiny-entry route being widened beyond one bounded authored slice
- capsule and authority surfaces collapsing into one interchangeable export layer

## Review result

- `yes` the export still keeps `examples/source_node.example.json` as the entry surface and does not replace ToS-authored authority
- `yes` the capsule and tiny-entry docs remain supporting surfaces rather than new canons
- `yes` the export stays bounded to the current Zarathustra prologue route and one bounded hop
- `yes` the new scripts validate export parity and keep the downstream surface reviewable

## What remains deferred

- wider corpus export coverage beyond the current Zarathustra prologue route
- broader source-owned export families for concept, lineage, or context nodes
- any graph-first or multi-hop export surface

## Boundary note

The source-owned KAG export remains a downstream-facing guide surface.

It may be consumed by `aoa-kag`, but it does not replace ToS-authored source
authority, node law, or the tree-first entry route inside Tree of Sophia.
