# 2026-03-23 Zarathustra Trilingual Entry Review

## What changed

- added a public bounded-route note for trilingual Zarathustra entry
- updated README, roadmap, knowledge model, and manual gate language to mark manual corpus movement as begun through one bounded route
- expanded the public node contract and schema with optional `language_witnesses` and `translation_tensions`
- turned `examples/source_node.example.json` into a real trilingual prologue entry with 2 shared sense blocks
- grounded `examples/concept_node.example.json` and `examples/concept_node_overcoming.example.json` more explicitly in that source route
- updated the review checklist for multilingual witness discipline

## Most at-risk checklist items

- source traceability under multilingual witness expansion
- one shared `node_id` versus language-split copies
- German source authority being replaced by translation convenience
- concept nodes collapsing into synonym piles
- bounded active source entry being mistaken for wider world-thought expansion

## Review result

- `yes` the changed surfaces keep a visible path back to German source authority
- `yes` the trilingual witness surface keeps one shared `node_id` rather than three language trees
- `yes` Russian and English remain witness layers rather than a new authored canon
- `yes` `becoming` and `overcoming` remain language-neutral concept nodes
- `yes` the first route stays bounded to 2 shared sense blocks from the prologue opening
- `yes` wider world-thought expansion remains later rather than being half-opened here

## Why these 2 sense blocks

The chosen blocks are the smallest cluster that already holds the needed movement:

- Zarathustra's heart changes
- wisdom becomes overflow
- descent and `untergehen` become active movement rather than simple defeat

That is enough to align `de / ru / en` witnesses and ground the first concept pair without inflating the first landing slice into a larger corpus claim.

## What remains interpretive or unresolved

- the Russian and English witness texts are bounded working layers around the German source rather than final canonical translations
- `untergehen` still carries more force than any single English or Russian rendering can exhaust, so that drift remains visible in `translation_tensions`
- the route now opens real tree motion, but it is still one bounded source entry rather than a broader multilingual branch program

## Intentionally deferred

- wider world-thought expansion
- cross-tradition multilingual growth
- KAG-facing multilingual projections
- translation automation
- additional concept nodes beyond `becoming` and `overcoming`

## Likely downstream follow-up

- later `Tree-of-Sophia` work if this bounded route stabilizes and a second source bundle becomes justified
- later `aoa-kag` work only after authored multilingual node law is clearer
