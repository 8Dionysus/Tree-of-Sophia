# Mode B Intake Stub

This path is reserved for raw candidate intake linked to the bounded
`thus-spoke-zarathustra/prologue-1` route.

At the current wave, this directory is only a scaffold.

It is meant for future raw tables such as:

- candidate node rows
- candidate relation rows
- bounded pass notes that remain explicitly provisional

Nothing here counts as source authority or canonical tree law by itself.
Primary witness material belongs in `sources/`.
Canonical authored nodes belong in `tree/`.
