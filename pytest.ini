[pytest]
testpaths = access/tests tests/test_validation_lanes.py
addopts = -m "not data_release"
markers =
    data_release: validates a separately selected full production data snapshot; requires TOS_DATA_ROOT
